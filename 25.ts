let alienColor: string = 'green';

// Version that passes the if test
if (alienColor === 'green') {
  console.log("Congratulations! You just earned 5 points.");
}

// Version that fails the if test (no output)
if (alienColor === 'red') {
  console.log("This condition will not be executed.");
}